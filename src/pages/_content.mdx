---
title: This is title
authors:
  - Author1
  - Author2
  - Author3
---

export const hello = "Hello World!";

export function sum(num1, num2) {
    return num1 + num2;
}

# Heading 1

{hello}

We can use `frontmatter` variable: {frontmatter.title}

Sum of 2 + 3 is {sum(2, 3)}

## Heading 2

<Callout>
  Custom component
</Callout>

{
  frontmatter.authors.map(author => (<div>{author}</div>))
}

`@shikijs/rehype` plugin:

```ts
import GitHubSlugger from 'github-slugger';

export default function createGitHubSlugger() {
  const slugger = new GitHubSlugger();

  return (text: string) => slugger.slug(text);
}
```